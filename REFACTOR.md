# 0.4.0 更新记录

日期：2026-09-12  
项目：F:/agent-status-bar-win-main

## 新增 Codex 多会话监控

移除最新线程 LIMIT 1 限制，批量读取各线程活动，关联全部未归档会话元数据。新增 rollout 生命周期解析，用于补充长时间任务、等待、完成和中断状态。新线程尚未写入元数据时，也可以从日志中发现。

每个会话按 thread ID 独立记录活动和通知；总状态汇总所有会话，不受结果顺序影响。标题、项目、短 ID、活动时间和主/子会话来源可在面板中查看，并支持活跃/待处理/全部筛选和搜索。

历史会话不会单凭全局存在 Codex 进程就被判为工作中；结合当前进程启动时间排除旧进程遗留记录。已归档或移除的会话消失时不伪造完成事件。

## 新增悬浮窗原位转场

同一个原生窗口在紧凑灯与完整面板之间切换，共用状态数据、设置和界面。悬停延迟 220 毫秒，离开延迟 320 毫秒，转场 240 毫秒。快速移出再返回可取消收起。按住拖动不触发展开，点击可展开；收起保留搜索条件和未保存的设置。

展开使用当前显示器工作区域定位，不抢焦点。面板关闭和 Escape 会回到紧凑灯。支持系统减少动画设置。

## 验证结果

- 43 项逻辑回归测试通过。
- 19 项 Electron 界面检查通过，含会话筛选/搜索、展开/收起、防抖、拖动抑制、收起后状态保留和原位还原。
- 7 项完整入口检查通过，含配置落盘、无效设置拒绝、桌面灯开关和重复启动。
- 实机扫描发现 119 个 Codex 会话，测试时其中 2 个工作中；不会把很久以前缺少完成记录的会话一并标为工作中。
- 中文界面与悬浮面板截图已做视觉检查。UI 控制台错误数为 0。
- 测试数据和截图保留在工作区副本，不恢复到已清理的 F 盘 artifacts 目录。

## 仍需理解的边界

“全部会话”指当前配置目录中可发现的未归档会话与新活动线程，不是全部 GUI 标签页。状态仍然由本地日志推断，无法精确绑定每个线程与 PID；rollout 读取有 256 KiB 尾部边界，日志缺失或格式变动时存在识别限制。详细说明见 README_zh.md。

## 回退与清理

写回前会保留本次修改涉及文件的备份，并保留原来的重构前备份。源码、测试和依赖继续放在原项目中；打包中间目录与验收截图留在工作区。退出旧版后打开 0.4.0 便携程序。

